@model int

<!-- esta vista parcial va a recibir el id y en el boton actualizar le paso por el helper asp](me permite pasar en la ruta el id),

lo que sigue despues del asp-route'-id' es el nomkbre con el que yo lo estoy identificando

el nombre debe ser exactamente el mismo que tenemos en el metodo upsert en bodegacontroller-->
<div class="d-grid gap-2 d-md-block">
    <button type="submit" class="btn btn-primary " onfocus="false" asp-route-id="@Model"> <i class="bi bi-pencil-square"></i> Actualizar</button>

    <a asp-action="Index" class="btn btn-success"> <i class="bi bi-arrow-return-left"></i> Regresar</a>

</div>